import 'package:flutter/material.dart';

class AppColors {
  // ───────── Primary ─────────
  static const Color primary = Color(0xFF0070C0);
  static const Color primaryDark = Color(0xFF005A9E);
  static const Color primaryLight = Color(0xFF4DA3E0);

  // ───────── Background ─────────
  static const Color background = Color(0xFF0B0F1A);
  static const Color surface = Color(0xFF111827);
  static const Color card = Color(0xFF1A2233);

  // ───────── Text ─────────
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textMuted = Colors.grey;

  // ───────── Status ─────────
  static const Color success = Color(0xFF00C853);
  static const Color error = Color(0xFFFF3B30);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF29B6F6);

  // ───────── Bluetooth ─────────
  static const Color bluetooth = Color(0xFF0070C0);
  static const Color bluetoothConnected = Color(0xFF00E676);
  static const Color bluetoothDisconnected = Color(0xFFFF3B30);

  // ───────── UI Elements ─────────
  static const Color border = Color(0xFF2A3441);
  static const Color disabled = Color(0xFF555555);
}
